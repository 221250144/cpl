#include<stdio.h>
#include<math.h>

int main()
{
    double pi;
    pi=4*(4*atan(1.0/5)-atan(1.0/239));
    printf("%.15f\n",pi);
    double p;
    p=(log(pow(640320,3)+744))/pow(163,1.0/2);
    printf("%.15f",p);

}
