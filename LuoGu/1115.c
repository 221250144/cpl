#include <stdio.h>

int main()
{
    int n = 0;
    scanf("%d",&n);
    int a[200005] = {0};
    for (int i = 0; i < n; ++i) {
        scanf("%d",&a[i]);
    }
    int max = a[0];
    
    return 0;
}