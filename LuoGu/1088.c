#include <stdio.h>

int main()
{
    int n = 0;
    int m = 0;
    scanf("%d%d",&n,&m);

    int a[10005] = {0};
    for (int i = 0; i < n; ++i) {
        scanf("%d",&a[i]);
    }
    long long num = 0;
    for (int i = 0; i < n; ++i) {
        num += a[i] *
    }
    return 0;
}
